1.创建基于styleName的权限控制.

如: 
	styleName="security() or security:"  //resouceString is id
	styleName="security(resourceString,controlBy) or security:resourceString"
	
2. remove的控制完善

3. 该客户端实现某个接口,以读取权限数据,如ISecurityMetadata.getSecurityMetadata()